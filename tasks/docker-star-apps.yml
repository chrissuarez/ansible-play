- name: Create folder for docker compose   
  file:
    path: /home/downloader/docker-compose      
    state: directory
    recurse: yes
  become: yes
  become_user: downloader

- name: copy docker-compose file
  copy:
    src: files/docker-compose.yml
    dest: /home/downloader/docker-compose/docker-compose.yml
    owner: downloader
    group: downloader

- name: Start containers
  docker_compose:
    definition: /home/downloader/docker-compose/docker-compose.yml
    project_name: myproject
    state: present